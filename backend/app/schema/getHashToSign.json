{
  "type": "object",
  "additionalProperties": false,
  "properties":
  { 
    "prev_hash": { "type" : "string", "minLength": 64, "maxLength": 66 },
    "prev_block" : { "type" : "integer", "minimum": 1 },
    "token_id" : { "type" : "string", "minLength": 70, "maxLength": 78 },
    "new_owner" : { "type" : "string", "minLength": 40, "maxLength": 42 }
  },
  "required": ["prev_block", "token_id", "new_owner", "prev_hash"]
}
